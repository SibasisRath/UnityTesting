using UnityEngine;

public class PenguinScript : MonoBehaviour
{
    private Transform penguinPosition;

    public Transform PenguinPosition { get => penguinPosition; set => penguinPosition = value; }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
